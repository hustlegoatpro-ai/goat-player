(()=>{"use strict";const STARTING_VOLUME=.78,STORAGE_KEY="goatPlayerVolume";const audio=document.getElementById("audio"),playButton=document.getElementById("play"),previousButton=document.getElementById("previous"),nextButton=document.getElementById("next"),seek=document.getElementById("seek"),volume=document.getElementById("volume"),volumeValue=document.getElementById("volumeValue"),currentTime=document.getElementById("currentTime"),duration=document.getElementById("duration"),journey=document.getElementById("journey");
const formatTime=s=>{if(!Number.isFinite(s)||s<0)return"0:00";const m=Math.floor(s/60),r=Math.floor(s%60);return`${m}:${String(r).padStart(2,"0")}`};
const updateGoaty=()=>{if(!Number.isFinite(audio.duration)||audio.duration<=0){journey.style.setProperty("--progress","4%");return}const ratio=audio.currentTime/audio.duration;journey.style.setProperty("--progress",`${4+ratio*80}%`)};
const saved=Number(localStorage.getItem(STORAGE_KEY)),initial=Number.isFinite(saved)?Math.min(1,Math.max(0,saved)):STARTING_VOLUME;audio.volume=initial;volume.value=String(initial);volumeValue.textContent=`${Math.round(initial*100)}%`;
volume.addEventListener("input",()=>{const v=Number(volume.value);audio.volume=v;volumeValue.textContent=`${Math.round(v*100)}%`;localStorage.setItem(STORAGE_KEY,String(v))});
playButton.addEventListener("click",async()=>{if(audio.paused){try{await audio.play()}catch(e){console.error("Audio playback failed:",e)}}else audio.pause()});
audio.addEventListener("play",()=>{playButton.textContent="Ⅱ";playButton.setAttribute("aria-label","Pause")});audio.addEventListener("pause",()=>{playButton.textContent="▶";playButton.setAttribute("aria-label","Play")});audio.addEventListener("loadedmetadata",()=>duration.textContent=formatTime(audio.duration));audio.addEventListener("timeupdate",()=>{if(!Number.isFinite(audio.duration)||audio.duration<=0)return;currentTime.textContent=formatTime(audio.currentTime);seek.value=String(Math.round(audio.currentTime/audio.duration*1000));updateGoaty()});
seek.addEventListener("input",()=>{if(Number.isFinite(audio.duration)&&audio.duration>0){audio.currentTime=Number(seek.value)/1000*audio.duration;updateGoaty()}});previousButton.addEventListener("click",()=>{audio.currentTime=0;updateGoaty()});nextButton.addEventListener("click",()=>{if(Number.isFinite(audio.duration)){audio.currentTime=Math.max(0,audio.duration-.05);updateGoaty()}});audio.addEventListener("ended",()=>{audio.currentTime=0;seek.value="0";currentTime.textContent="0:00";updateGoaty()});audio.addEventListener("error",()=>console.error("Could not load demo-tone.wav. Confirm it is in the repository root."));updateGoaty()})();
